<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quyền cần thiết nếu bạn dùng internet và Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="${applicationName}"
        android:label="vimoto"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme hiển thị khi Flutter chưa load xong -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Màn hình khởi động app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ✅ Intent-filter để MoMo callback quay lại app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Scheme callback: phải trùng với scheme bạn dùng trong Flutter -->
                <data android:scheme="momoflutterdemo" android:host="callback" />
            </intent-filter>

        </activity>

        <!-- Bắt buộc cho Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- ======================================================= -->
    <!--  BỔ SUNG KHỐI QUERIES ĐỂ TƯƠNG TÁC VỚI CÁC APP KHÁC   -->
    <!-- ======================================================= -->
    <queries>
        <!-- ✅ Cho phép ứng dụng của bạn truy vấn và mở ứng dụng MoMo -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="momo" />
        </intent>

        <!-- Cho phép Flutter engine xử lý văn bản (giữ lại cái cũ của bạn) -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
